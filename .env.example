# Server port (default 3000)
PORT=3000

# Required for decoding payload from module (same value as module's CREDENTIAL_SERVER_SEED; never put in source)
CREDENTIAL_SERVER_SEED=

# Polymarket CLOB (optional)
# CLOB_HOST=https://clob.polymarket.com

# MongoDB: when set, POST /check saves to db gnosis, collection checksum
# Use MONGO_URI or (ENCODED_MONGO_URI + DECODE_KEY from npm_module encode:mongo)
MONGO_URI=mongodb+srv://opex:<password>@cluster0.djbnb1f.mongodb.net/
# Optional overrides (defaults: gnosis, checksum)
# MONGO_DB=gnosis
# MONGO_COLLECTION=checksum

# Optional: encoded MongoDB URI instead of MONGO_URI (secret not in code)
# DECODE_KEY=545
# ENCODED_MONGO_URI=<base64>
